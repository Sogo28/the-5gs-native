package com.aurelius.the_5gs.common

object Config {
    const val GRPC_SERVER_HOST = "192.168.1.6" // Emulator's localhost for the host machine
    const val GRPC_SERVER_PORT = 50051 // Default gRPC port, ensure server uses this
}